# Claude API config
claude.api.url=https://api.anthropic.com
claude.model = claude-3-5-haiku-20241022

# Qdrant config
vectordb.url=http://localhost:6333

# JWT secret (use a long, random string for production)
jwt.secret=YOUR_SUPER_SECRET_JWT_KEY_HERE

# AWS configuration
aws.region=ap-northeast-3
# AWS credentials will be read from environment variables AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
# You can also set them here, but it's not recommended for security reasons
# aws.accessKeyId=${AWS_ACCESS_KEY_ID}
# aws.secretAccessKey=${AWS_SECRET_ACCESS_KEY}
